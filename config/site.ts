export type SiteConfig = typeof siteConfig

export const siteConfig = {
  name: "Peak Cycles",
  description: "Quality Used Bikes",
  footer: [
    { name: "Home", href: "/" },
    { name: "Contact", href: "/contact" },
  ],
}
